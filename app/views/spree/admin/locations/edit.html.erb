<div class="container">
	<div class="col-md-12 col-lg-12 no-pad">
		<a href="/catalog/admin/locations"><i class="fa fa-arrow-left"></i> Locations Dashboard </a> <a href=<%= "/catalog/admin/locations/#{@location.store_num}" %> ><i class="fa fa-arrow-left"></i> <%= " #{@location.store_name}" %> </a><br><br>
	</div>
	<div class="col-md-12 col-lg-12">
			<%= form_for [:admin, @location] do |f| %>
			    <%= render :partial => 'spree/admin/locations/form', :locals => { :f => f, states: @states } %>
			    <p><%= f.submit Spree.t(:update), :class => 'button-primary admin-content' %></p>
			<% end %>
		</div>
	</div>	
</div>